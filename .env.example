# Copy to .env.local for local overrides. Never commit real values.
# CDK / deploy (used later; not required for pnpm dev)
# CDK_DEFAULT_ACCOUNT=
# CDK_DEFAULT_REGION=us-east-1
# DOMAIN=playlang.vazue.com
# HOSTED_ZONE_NAME=vazue.com
