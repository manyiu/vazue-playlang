/** Pinned npm package version (CDN assets + PhpWeb API). */
export const PHP_WASM_PKG_VERSION = "0.1.0";
/** PHP series served by the default PhpWeb binary. */
export const PHP_LANGUAGE_VERSION = "8.4";
export const PHP_WASM_CDN_BASE =
  `https://cdn.jsdelivr.net/npm/php-wasm@${PHP_WASM_PKG_VERSION}/`;
